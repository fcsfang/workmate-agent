# Workmate Agent Roadmap

Workmate Agent 的长期目标，是成为一个能持续监督用户行为、帮助用户集中注意力完成任务、提高生产力和专注度，并支持用户建立自律生活节奏的个人行动 Agent。

这份路线图用于把愿景拆成可执行的版本方向。它不替代 `CHANGELOG.md`：`CHANGELOG.md` 记录已经完成的版本，`ROADMAP.md` 记录接下来要朝哪里走。

如果开发目标切换为“把 Workmate Agent 做成可以放入简历的 Agent 工程项目”，后续具体开发顺序以 [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md) 为准。

## 产品原则

- 陪伴优先：先接住用户，再整理任务，最后才给轻提醒。
- 低压力监督：监督不是催促、审问或要求证明，而是在合适时机帮助用户回到主线。
- 行为闭环：任务、专注、承诺、复盘和提醒要形成可追踪的闭环，而不是散落的聊天记录。
- 长期观察：比起单次建议，更重视持续识别用户的行为模式，例如拖延、过度规划、频繁切换任务和专注中断。
- 用户可控：提醒频率、通知渠道、数据保留和主动监督强度应逐步可配置。
- 本地优先：早期继续保持本地 JSON 记忆和本地 Web 体验，避免引入过重基础设施。

## 当前基础

截至 V0.9.2，项目已经具备：

- 长期记忆、语义压缩、自我反省和记忆治理
- 任务生命周期、子任务、可视化 Todo List 和任务相似度去重
- 专注会话、承诺 deadline、行为统计、早间简报、晚间复盘和周度诊断
- 浏览器通知、后台 scheduler、多通道推送和推送自检
- 内部状态工具层，用于读写任务、承诺、记忆和专注会话

这些能力已经构成了“持续行动监督 Agent”的雏形。后续重点不只是继续加功能，而是让这些模块真正联动起来，形成稳定、低摩擦、可长期使用的行为支持系统。

## 近期优先级

### V1.0 主动监督闭环

目标：让 Agent 不只是记录状态，而是能围绕任务、专注和承诺形成一套稳定的主动监督闭环。

要做：

- 统一监督事件模型：把专注超时、承诺到期、任务停滞、任务过散、连续低活跃等情况抽象为 `supervision_events`
- 为每类事件定义状态流：`detected / notified / acknowledged / snoozed / resolved / muted`
- 避免重复打扰：同一事件在用户确认、处理或静音前不反复推送
- 支持用户对提醒反馈：例如“已完成”“稍后提醒”“今天别提醒”“这个不重要”
- 将 Web 通知、后台推送和 Agent 对话回复统一到同一套事件状态

验收标准：

- 用户离开后，专注超时或承诺到期能被识别并提醒
- 用户回来后，Agent 能知道提醒是否已经发过、是否被稍后提醒、是否需要收束
- 同一件事不会在短时间内被多次重复催促
- 提醒语气仍然保持低压力和可选择性

暂不做：

- 不做复杂日历系统
- 不做强制打卡
- 不把提醒变成惩罚或评分

### V1.1 行为模式分析

目标：从“发生了什么”升级为“用户经常怎样行动”，帮助用户看见自己的行为模式。

基础已实现：

- 新增 `BehaviorPatternManager`
- 新增 `memory/data/behavior_patterns.json`
- 从任务、专注会话、承诺和监督事件中提炼轻量行为模式
- 当前已覆盖专注完成率偏低、专注超时、承诺积压、承诺履行率偏低、任务分散、任务停滞、提醒摩擦和稳定回访节奏
- 行为模式进入 Web 的 `MEMORY / patterns` 区域，并在任务、监督、复盘和周报场景进入上下文

继续要做：

- 引入 LLM 辅助提炼，减少纯规则分析的生硬感
- 继续识别常见模式：反复规划、过度输入、任务切换频繁、承诺过多、完成后不收束
- 为每种模式记录证据片段、出现频率、最近发生时间和建议干预方式
- 将模式分析接入周报和上下文注入，但避免每轮都提醒

验收标准：

- 周报能引用真实行为数据，而不只是泛泛建议
- Agent 能区分偶发状态和长期模式
- 用户在被提醒时能看到“为什么 Agent 这样判断”

暂不做：

- 不做心理诊断
- 不给用户贴固定标签
- 不用单次行为推断长期性格

### V1.2 个人自律仪表盘

目标：把用户的行动状态可视化，让用户一眼看到当前主线、今天进展和本周节奏。

基础已实现：

- 新增 `DashboardManager`
- 新增 GET `/api/dashboard`
- `EXECUTION` 顶部新增 `TODAY DASHBOARD`
- 展示今日专注次数/分钟数、完成任务数、关闭承诺数、未关闭承诺、到期承诺、当前主线、任务分散度、本周专注时长、本周任务完成和活跃天数
- 给出一条“今日建议只看一眼”的轻提示，不做复杂评分
- 支持快速操作：用当前主线开始专注、完成当前主线、跳转查看监督事件

继续要做：

- 增加更清晰的周趋势可视化
- 支持从仪表盘直接关闭到期承诺或稍后提醒监督事件
- 将仪表盘与后续日/月复盘连接起来

验收标准：

- 用户打开页面后，不需要翻聊天记录就能知道现在该回到哪里
- 仪表盘能反映真实本地记忆数据
- 操作后能同步更新任务、专注和承诺状态

暂不做：

- 不做重型数据分析平台
- 不做游戏化排行榜
- 不引入社交比较

### V1.3 提醒策略个性化

目标：让主动监督更贴合用户，而不是所有人都用同一套提醒频率和语气。

基础已实现：

- 增加提醒偏好配置：提醒强度、静默时段、默认稍后提醒时长、推送最低等级、事件类型开关
- 基于 `feedback_stats` 生成自适应提醒策略建议
- 当用户频繁稍后或静音提醒时，建议提高推送门槛或延长稍后提醒间隔
- 当用户能稳定确认或关闭提醒时，建议适度恢复中等级推送
- 在用户压力高、低能量或深夜/清晨休息时段，自动建议降低监督语气和主动推送强度
- 将用户长期画像中的沟通偏好用于提醒文案，优先生成低压力、短句、不要求证明的 `display_message`
- 按渠道设置不同提醒门槛：页面内展示、浏览器通知、后台/手机推送可以分别使用不同严重度
- 根据不同监督事件类型的长期反馈调整门槛，例如某类提醒经常被静音时，只降低这一类提醒的主动推送强度
- Web 端提供 `APPLY STRATEGY`，用户确认后才应用建议
- 支持“只提醒承诺”“只提醒专注”“只提醒任务”“今天安静一点”“暂停提醒”“恢复提醒”等轻量自然语言控制，并升级为 LLM 优先分类、规则兜底

继续要做：

- 让提醒策略逐步参考更多任务语义和时间段反馈，例如学习/编码/杂事分别更适合哪种提醒

验收标准：

- 用户可以明确控制提醒边界
- Agent 不会在不合适的时间制造额外压力
- 提醒策略能随着用户反馈变得更贴合

暂不做：

- 不做复杂权限系统
- 不做多用户组织管理
- 不做硬性惩罚机制

## 中期方向

### 更可靠的记忆检索

- 引入可选向量检索，减少纯关键词搜索的漏召回
- 为不同记忆类型设置不同检索策略，例如任务、承诺、偏好、行为模式分别召回
- 增加记忆质量评分，降低过期、冲突或低价值记忆的上下文权重
- 保持本地可运行，向量检索作为可选增强

### 更成熟的任务层级

- 支持主任务、子任务、阶段目标和长期目标的层级关系
- 区分“用户明确提出的任务”和“Agent 建议的下一步”
- 避免把建议误写成用户承诺
- 支持任务归档、延期和恢复

### 更自然的主动陪伴

- 将主动消息从“提醒”扩展到“收束”“复盘”“欢迎回来”“轻量启动”
- 让 Agent 能根据时间、用户状态和历史节奏选择合适的主动语气
- 默认不追问，除非确实需要用户给一个关键信息

### 更低门槛的分发

- 保持 `./run.sh` 一键启动
- 后续可考虑桌面 App、托盘常驻或 Docker 封装
- 简化 `.env` 配置和推送渠道配置
- 提供示例数据或新手引导，帮助首次使用者理解产品节奏

## 长期方向

- 跨设备同步：让手机提醒、桌面页面和本地记忆状态保持一致
- 日/月/季度复盘：帮助用户看见更长周期的行为变化
- 目标系统：将长期目标、阶段目标、当前任务和今日行动连成一条线
- 数据可控：支持导出、清理、归档和迁移个人记忆
- 本地优先的隐私设计：尽量让用户的重要行为数据留在本地或可控存储中

## 下一步建议

当前已经从 V1.0 主动监督闭环推进到 V1.3 提醒策略个性化。V1.0 已经补上统一监督事件模型、稍后提醒、提醒偏好、反馈统计，以及监督事件与任务/承诺/专注状态的轻量联动；V1.1 已经补上轻量行为模式层；V1.2 已经补上轻量 dashboard 状态聚合和 Web 顶部仪表盘；V1.3 已经补上基于用户反馈的提醒策略建议层、LLM 优先的自然语言提醒控制、压力/低能量感知的提醒语气策略、长期画像驱动的提醒文案策略、分渠道提醒门槛，以及按监督事件类型学习的提醒门槛。

V1.0 已实现的基础闭环：

- `SupervisionEventManager`
- `memory/data/supervision_events.json`
- `/api/supervision/events`
- Web 侧提醒记录与静音/确认/关闭操作
- `snooze` 稍后提醒状态
- 用户提醒偏好配置，例如提醒总开关、静默时间段、默认稍后提醒时长、按事件类型启停提醒
- `push_min_severity` 推送最低等级，让低级提醒可以只在页面显示
- 监督事件反馈统计，用于观察用户更常确认、稍后提醒、静音还是关闭提醒
- `DONE` 操作会尝试同步收束关联的 Focus Session、Commitment 或 Todo
- scheduler 改为写入并更新监督事件，而不是只维护内存里的已通知集合

建议下一轮继续实现：

- 将行为模式分析从规则版升级为 LLM 辅助版，提升自然度和解释质量
- 继续增强 V1.2 仪表盘，例如周趋势可视化、到期承诺快速关闭、监督事件稍后提醒入口
- 继续增强 V1.3，让提醒策略逐步参考更多长期反馈信号，并为后续跨设备监督体验打基础

这会继续把 Workmate Agent 从“有提醒能力”推进到“有用户可控、可追踪、可调节的主动监督闭环”。
